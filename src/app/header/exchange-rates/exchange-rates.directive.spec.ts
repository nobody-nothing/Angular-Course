import { ExchangeRatesDirective } from './exchange-rates.directive';

describe('ExchangeRatesDirective', () => {
  it('should create an instance', () => {
    const directive = new ExchangeRatesDirective();
    expect(directive).toBeTruthy();
  });
});
