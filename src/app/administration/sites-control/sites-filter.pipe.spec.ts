import { SitesFilterPipe } from './sites-filter.pipe';

describe('SitesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SitesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
